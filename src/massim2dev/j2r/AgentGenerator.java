package massim2dev.j2r;

import org.eclipse.core.commands.ExecutionEvent;
import org.eclipse.core.resources.IProject;
import org.eclipse.core.runtime.CoreException;

/**
 * This class is used to create a JADE agent class from
 * a Repast agent class. Its methods are static. To get
 * started, simply call the method "generate(...)"
 * and pass it an agent class object to have it converted.
 * Alternatively, call the method "generateAll(...)"
 * @author joaolopes
 *
 */
public class AgentGenerator {
	
	public static Object execute(ExecutionEvent event, IProject currentProject) {
		 
		// 
		
		return null;
	}

	
}
